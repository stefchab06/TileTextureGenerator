<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TileTextureGenerator.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:TileTextureGenerator.Presentation.UI.Pages;assembly=TileTextureGenerator.Presentation.UI"
    Shell.NavBarIsVisible="False">

    <!-- Main/startup page -->
    <ShellContent
        ContentTemplate="{DataTemplate pages:ManageProjectListPage}"
        Route="ManageProjectListPage" />

    <!-- Note: EditProjectPage is now registered as a custom route in AppShell.xaml.cs -->

</Shell>
